= link_to 'Sign in', new_user_session_path
%br
= link_to 'Sign up', new_user_registration_path
- if @laws.empty?
  %p No laws to show; run `rake db:seed` and reload this page.
- else
  %ul
  - @laws.all.each do |law|
    %li
      %a{href: law_path(law.id)}= law.name
