.row.registration
  .col-sm-offset-4.col-sm-4
    %h2 Breyting á lykilorði
    = form_for(resource, as: resource_name, url: password_path(resource_name), html: {method: :put}) do |f|
      = f.hidden_field :reset_password_token
      .form-group= f.password_field :password, autofocus: true, placeholder: 'Nýtt lykilorð', required: true, class: 'form-control'
      .form-group= f.password_field :password_confirmation, placeholder: 'Nýtt lykilorð (aftur)', required: true, class: 'form-control'
      .form-group= f.submit 'Breyta lykilorði', class: 'btn btn-primary form-control'
